# ark-filecheck

# How to use
Either install python and run the .py file (some packages may need installing) and enjoy!
Any other file paths / registry things I am missing lmk! 

Ensure any configs are saved to a safe location before running.
The file automatically deletes after one use, so should be undetectable.
